{
  "name": "node-lens-logging",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/logging/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "build-server",
        "build-client"
      ],
      "options": {
        "command": "echo built node-lens-logging"
      }
    },
    "build-server": {
      "executor": "nx:run-commands",
      "outputs": [
        "{projectRoot}/dist/src/server"
      ],
      "dependsOn": [
        "^build"
      ],
      "options": {
        "command": "tsc --build packages/logging/tsconfig.server.json"
      }
    },
    "build-client": {
      "executor": "@nx/vite:build",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "packages/logging/dist/src/client",
        "configFile": "packages/logging/vite.config.mts",
        "tsConfig": "packages/logging/tsconfig.client.json",
        "generatePackageJson": false
      }
    }
  }
}
